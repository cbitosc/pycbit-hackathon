print("Prime numbers between 1 and 99999 are:")

for num in range(1,100000):
  
   if num > 1:
       for i in range(2,num):
           if (num % i) == 0:
               break
       else:
           print(num)
